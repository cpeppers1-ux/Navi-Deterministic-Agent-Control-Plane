{
  "name": "navi",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal local-first control plane for AI-assisted development.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "navi": "npm run build --silent && node dist/src/cli.js",
    "test": "npm run build --silent && node --test dist/tests/core.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
